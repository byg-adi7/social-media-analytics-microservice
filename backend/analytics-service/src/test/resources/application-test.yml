spring:
  datasource:
    url: jdbc:h2:mem:analytics_test_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      # validate (not create-drop): Flyway's V1 migration creates the
      # schema; this confirms the entity mapping actually matches what
      # Flyway produces, rather than Hibernate silently generating its own
      # schema over it and masking a real mismatch.
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  jackson:
    default-property-inclusion: non_null

supabase:
  jwt-secret: test-supabase-secret-for-tests-only

analytics:
  sync:
    enabled: false
  mock-data:
    enabled: true

youtube:
  enabled: false
  redirect-uri: http://localhost:8082/api/oauth/youtube/callback
  frontend-redirect-uri: http://localhost:3000/dashboard

spotify:
  enabled: false
  redirect-uri: http://localhost:8082/api/oauth/spotify/callback
  frontend-redirect-uri: http://localhost:3000/dashboard

instagram:
  enabled: false
  redirect-uri: http://localhost:8082/api/oauth/instagram/callback
  frontend-redirect-uri: http://localhost:3000/dashboard

tiktok:
  enabled: false
  redirect-uri: http://localhost:8082/api/oauth/tiktok/callback
  frontend-redirect-uri: http://localhost:3000/dashboard

facebook:
  enabled: false
  redirect-uri: http://localhost:8082/api/oauth/facebook/callback
  frontend-redirect-uri: http://localhost:3000/dashboard

oauth:
  state-secret: test-secret

# Disabled so test request volume never depends on incidental headroom
# under the default capacity - tests aren't exercising rate-limit behavior.
rate-limit:
  enabled: false

logging:
  level:
    root: WARN
